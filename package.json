{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
"devDependencies": {
    "react-scripts": "5.0.1" // The version number might differ
},
"scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build", // This is the command Vercel is failing on
    "test": "react-scripts test",
    "eject": "react-scripts eject"
}
}
